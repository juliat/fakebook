<%= form_for(@political_view) do |f| %>
  <% if @political_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@political_view.errors.count, "error") %> prohibited this political_view from being saved:</h2>

      <ul>
      <% @political_view.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
